name: Reproduce Python Buildless Capture (Windows)

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Coverity thin client version (e.g. 2026.6.0)'
        required: false
        default: '2026.6.0'

jobs:
  reproduce:
    runs-on: windows-latest

    env:
      COV_VERSION: ${{ inputs.version || '2026.6.0' }}
      # repo.blackduck.com credentials (Settings → Secrets → ARTIFACTORY_USER / ARTIFACTORY_TOKEN)
      ARTIFACTORY_USER: ${{ secrets.ARTIFACTORY_USER }}
      ARTIFACTORY_TOKEN: ${{ secrets.ARTIFACTORY_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: System Info
        run: |
          python --version
          Get-Command python | Select-Object Source
          chcp
          [System.Globalization.CultureInfo]::CurrentCulture
          [System.Globalization.CultureInfo]::CurrentUICulture

      - name: Download Coverity Thin Client (repo.blackduck.com)
        run: |
          $filename = "cov-thin-client-win64-${env:COV_VERSION}.tar.gz"
          $url = "https://repo.blackduck.com/artifactory/coverity-releases/${env:COV_VERSION}/${filename}"
          Write-Host "Downloading: $url"
          curl -fLsS `
            -u "${env:ARTIFACTORY_USER}:${env:ARTIFACTORY_TOKEN}" `
            -o "${{ runner.temp }}\${filename}" `
            "$url"
          New-Item -ItemType Directory -Force -Path "${{ runner.temp }}\coverity" | Out-Null
          tar -xzf "${{ runner.temp }}\${filename}" -C "${{ runner.temp }}\coverity"
          Write-Host "Extracted:"
          Get-ChildItem "${{ runner.temp }}\coverity" -Depth 2 | Select-Object FullName

      - name: Run Capture (debug)
        run: |
          $covBin = Get-ChildItem -Path "${{ runner.temp }}\coverity" -Recurse -Filter "coverity.exe" |
            Select-Object -First 1
          if (-not $covBin) { Write-Error "coverity.exe not found"; exit 1 }
          Write-Host "Using: $($covBin.FullName)"
          $covDir = $covBin.DirectoryName
          $env:PATH = "$covDir;$env:PATH"

          Write-Host "=== coverity capture --project-dir . ==="
          coverity -V capture --project-dir . 2>&1
          Write-Host "Capture exit: $LASTEXITCODE"

          Write-Host "=== coverity list (captured files) ==="
          coverity list 2>&1
          Write-Host "List exit: $LASTEXITCODE"

          Write-Host "=== .coverity dir contents ==="
          Get-ChildItem -Recurse .coverity -ErrorAction SilentlyContinue | Select-Object FullName | head -30

      - name: Upload coverity capture output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverity-capture-output
          path: .coverity/
          if-no-files-found: warn
